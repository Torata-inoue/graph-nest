import React from "react";
import { TaskPanesProps } from "./TaskPaneRouter.tsx";

const Confirm: React.FC<TaskPanesProps> = ({
  title,
  setRouteNum,
  formMethods,
}) => {
  return "confirm";
};

export default Confirm;
